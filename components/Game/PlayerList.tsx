/**
 * PlayerList Component
 * 
 * Responsibility: Display list of booked players with leave buttons
 * Props:
 *   - bookings: Array<{ id, player_name }>
 *   - onLeaveGame: (bookingId: number) => Promise<void>
 * 
 * Renders:
 *   - PlayerBadge for each booking
 *   - Leave button inside each PlayerBadge
 * 
 * Usage: Inside GameCard to show who's booked for that game
 */

// Component to be implemented
