/**
 * GameCard Component
 * 
 * Responsibility: Display single game info (title, location, time, price)
 * Props:
 *   - game: { id, title, location, time, price }
 *   - children?: React.ReactNode (for booking form slot)
 * 
 * Usage: Wraps game details in the home page list
 * Contains: GameCard header with title, location
 */

// Component to be implemented
