/**
 * BookingForm Component
 * 
 * Responsibility: Name input + Book button for a specific game
 * Props:
 *   - gameId: number
 *   - playerName: string
 *   - onPlayerNameChange: (name: string) => void
 *   - onBookGame: (gameId: number, playerName: string) => Promise<void>
 *   - isLoading?: boolean
 * 
 * Renders:
 *   - FormInput for player name
 *   - Button to submit booking
 * 
 * Usage: Inside GameCard for each game in the home page
 */

// Component to be implemented
