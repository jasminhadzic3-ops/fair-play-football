/**
 * PlayerBadge Component
 * 
 * Responsibility: Display individual player avatar + name + leave button
 * Props:
 *   - playerName: string
 *   - bookingId: number
 *   - onLeave: (bookingId: number) => void
 * 
 * Renders:
 *   - Avatar circle with player initial
 *   - Player name text
 *   - Leave button
 * 
 * Usage: Used inside PlayerList for each booked player
 */

// Component to be implemented
