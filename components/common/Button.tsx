/**
 * Button Component
 * 
 * Responsibility: Reusable button with variants (primary, danger, secondary)
 * Props:
 *   - children: React.ReactNode
 *   - onClick: () => void | Promise<void>
 *   - variant?: "primary" | "danger" | "secondary" (default: "primary")
 *   - disabled?: boolean
 *   - fullWidth?: boolean
 * 
 * Usage: "Create Game", "Book", "Leave Game", "Submit" buttons
 */

// Component to be implemented
